import React, { PureComponent } from 'react';
import { WriterWrapper, WriterWrapperItem } from '../style';

class Writer extends PureComponent {
    render() {
        return (
            <WriterWrapper>
                <WriterWrapperItem></WriterWrapperItem> 
            </WriterWrapper>
        );
    }
}

export default Writer;
